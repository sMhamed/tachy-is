package de.axone.repository;

import de.axone.model.InstallationTire;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InstallationTireRepository extends JpaRepository<InstallationTire, Long> {
}
